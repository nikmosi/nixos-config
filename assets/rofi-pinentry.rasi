/*
 * ┌─────────────────────────────────────────────────────────────┐
 * │  pinentry-rofi · "Vault" theme                              │
 * │  Catppuccin Macchiato palette · purple accent                │
 * │  Designed for GPG passphrase entry                          │
 * └─────────────────────────────────────────────────────────────┘
 *
 * Visibility trick: rofi password-mode draws • dots using
 * entry.text-color. Setting it to a bright accent (#8bd5ca teal)
 * on a dark inputbar background makes typed characters obvious.
 */

* {
    bg:          #24273a;
    bg-alt:      #363a4f;
    bg-hover:    #494d64;
    fg:          #cad3f5;
    fg-dim:      #6e738d;
    blue:        #8aadf4;
    green:       #a6da95;
    red:         #ed8796;
    yellow:      #eed49f;
    cyan:        #8bd5ca;
    purple:      #c6a0f6;

    background-color: transparent;
    text-color:       @fg;
    border-color:     @bg-alt;
    margin:  0px;
    padding: 0px;
    spacing: 0px;
}

/* ── Window ─────────────────────────────────────────────────── */
window {
    width:        840px;
    height:       320px;
    padding:      0px;
    border:       2px;
    border-radius: 20px;
    background-color: @bg;
    border-color:     #eed49f;
}

/* ── Mainbox ────────────────────────────────────────────────── */
mainbox {
    padding: 24px;
    spacing: 16px;
    orientation: vertical;
}

/* ── Message (description / error) ──────────────────────────── */
message {
    padding:       14px 18px;
    border-radius: 14px;
    background-color: @bg-alt;
    border:        1px;
    border-color:  @bg-hover;
}
textbox {
    text-color:  @fg;
    font:        "JetBrainsMono Nerd Font Mono 12";
    horizontal-align: 0.5;
    vertical-align:   0.5;
}

/* ── Inputbar ───────────────────────────────────────────────── */
inputbar {
    padding:        16px 20px;
    spacing:        12px;
    border-radius:  14px;
    background-color: @bg-alt;
    border:         1px;
    border-color:   @bg-hover;
    children:       [ "prompt", "entry", "case-indicator" ];
}

/* ── Prompt (lock icon + label) ─────────────────────────────── */
prompt {
    text-color:  @purple;
    font:        "JetBrainsMono Nerd Font Mono 15";
    /*  = nf-md-lock  */
    str:         "  Passphrase";
}

/* ── Entry (password input) ─────────────────────────────────── */
entry {
    placeholder:        "Enter passphrase…";
    placeholder-color:  @fg-dim;
    /* BRIGHT accent so • dots are highly visible in password mode */
    text-color:         @cyan;
    font:               "JetBrainsMono Nerd Font Mono 15";
    cursor:             text;
    cursor-color:       @cyan;
    cursor-width:       2px;
    blink:              true;
}

/* ── Case indicator (caps lock warning) ─────────────────────── */
case-indicator {
    text-color: @fg-dim;
    font:       "JetBrainsMono Nerd Font Mono 15";
    /*  = nf-md-keyboard-caps */
    str:        "";
}

/* ── Listview (empty in password mode, but styled for confirm) */
listview {
    padding:      4px 0px 0px;
    spacing:      6px;
    lines:        3;
    columns:      1;
    fixed-height: false;
    scrollbar:    false;
    border:       0px;
}

element {
    padding:       10px 14px;
    spacing:       10px;
    border-radius: 10px;
    cursor:        pointer;
    font:          "JetBrainsMono Nerd Font Mono 13";
}
element normal.normal {
    background-color: transparent;
    text-color:       @fg;
}
element selected.normal {
    background-color: @purple;
    text-color:       @bg;
}
element alternate.normal {
    background-color: transparent;
    text-color:       @fg;
}
element-icon {
    size:             20px;
    background-color: transparent;
    text-color:       inherit;
}
element-text {
    background-color: transparent;
    text-color:       inherit;
    vertical-align:   0.5;
}

/* ── Error state (when pinentry sends error message) ────────── */
error {
    text-color: @red;
}
